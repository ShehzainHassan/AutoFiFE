export type SectionTitleProps = {
  title: string;
  buttonText?: string;
  backgroundColor?: string;
  showButton?: boolean;
  ratingsText?: string;
  color?: string;
  padding?: string;
  onClick?: () => void;
};
