export type SectionTitleProps = {
  title: string;
  titleId?: string;
  buttonText?: string;
  backgroundColor?: string;
  showButton?: boolean;
  ratingsText?: string;
  color?: string;
  onClick?: () => void;
};
